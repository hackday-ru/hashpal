export default class hackTagParser {
    constructor() {
        this.hasTags = (text) => {
            //return tags
        };
        this.cropTags = (text) => {
          //return tags && text w/o tags  
        }
    }
}